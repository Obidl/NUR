# Local API
VITE_API_BASE_URL=http://localhost:4000

# Optional — browser Sentry DSN (separate project from API recommended).
# VITE_SENTRY_DSN=

# Production (Vercel): leave empty so the browser calls same-origin /api and /health
# (rewritten to Render in vercel.json). Avoids CORS / CORP / SSL flakiness.
# VITE_API_BASE_URL=
